<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Editar un anime</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  </head>
  <body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
  <nav class="navbar navbar-expand-lg-dark bg-body-tertiary bg-dark border-bottom border-body" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">ANIME LIST</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Lista animes</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/create">Crear un nuevo anime</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <% if (anime) { %>
    <form id="form" action="/update-image" method="POST" enctype="multipart/form-data">
      <input class="form-control" id="id" name="id" type="text" value="<%= anime.id %>" hidden>
      <div class="container">
        <h2 class="mt-4 mb-3 text-center">Editar un anime</h2> 
        <div class="mb-3">
          <label class="form-label" for="nombre">Nombre</label>
          <input class="form-control" id="nombre" name="nombre" type="text" value="<%= anime.nombre %>">
      </div>
      <div class="mb-3">
          <label class="form-label" for="demografia">demografia</label>
          <select name="demografia" id="demografia" aria-label="default select for demografia" class="form-select">
              <option <%= anime.demografia == 'shoujo' ? "selected" : '' %> value="shoujo">Shoujo</option>
              <option <%= anime.demografia == 'shounen' ? "selected" : '' %> value="shounen">Shounen</option>
              <option <%= anime.demografia == 'josei' ? "selected" : '' %> value="josei">Josei</option>
              <option <%= anime.demografia == 'seinen' ? "selected" : '' %> value="seinen">Seinen</option>
              <option <%= anime.demografia == 'kodomo' ? "selected" : '' %> value="kodomo">Kodomo</option>
              <option <%= anime.demografia == 'spokon' ? "selected" : '' %> value="spokon">Spokon</option>
          </select>
      </div>
      <div class="mb-3">
        <label class="form-label" for="descripcion">Descripcion</label>
        <textarea class="form-control" id="descripcion" name="descripcion" type="text"><%= anime.descripcion %></textarea>
    </div>
    <div class="mb-3">
      <% if(anime.imagen){ %>
        <img src="<%= anime.imagen %>" class="rounded mx-auto d-block img-thumbnail" alt="<%= anime.nombre %>">
      <% } %>
        <label class="form-label mt-4" for="photo">Actualizar Imagen:</label>
        <input class="form-control" type="file" name="photo" id="photo"/>
    </div>
      <button class="btn btn-primary mt-2" type="submit">Guardar</button>
      <a href="/" class="btn btn-secondary mt-2">Cancelar</a>
    </form>
    </div>
    <% } %>
</html>